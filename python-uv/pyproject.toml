[project]
name = "python-uv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "core",
]

[tool.uv.workspace]
members = ["packages/core", "packages/functions"]

[tool.uv.sources]
core = { workspace = true }
